{
  "name": "maxwell-asset-creator",
  "version": "1.0.0",
  "description": "Bedrock Action Group tool Lambda for Maxwell — creates assets (tools/parts) with initial state observations",
  "main": "index.js",
  "scripts": {
    "test": "vitest",
    "test:run": "vitest --run",
    "test:coverage": "vitest --coverage"
  },
  "devDependencies": {
    "fast-check": "^4.9.0",
    "vitest": "^4.1.4"
  }
}
